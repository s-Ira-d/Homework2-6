// math.js — модуль із функціями для обчислень

export function add(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}

// експорт за замовчуванням
export default function square(x) {
  return x * x;
}
